# Python bytecode / caches
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# Build artifacts
build/
dist/
*.egg-info/

# Environment / secrets
.env
.env.*

# Local provider config (user-specific; secrets-free but machine-local,
# see providers.example.yaml for the committed template)
providers.yaml

# Run artifacts (never commit run results or generated fixtures)
/results/
/fixtures/generated/

# OS noise
.DS_Store
